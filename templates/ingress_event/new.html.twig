{% extends 'base.html.twig' %}

{% block title %}New IngressEvent{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('helper/editor') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('helper/editor') }}
{% endblock %}

{% block body %}
    <a class="float-right btn btn-outline-secondary"
       href="{{ path('ingress_event_index') }}">{{ 'Back to list'|trans }}</a>
    <h1>{{ 'Create new IngressEvent'|trans }}</h1>

    {{ include('ingress_event/_form.html.twig') }}

{% endblock %}
