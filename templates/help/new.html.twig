{% extends 'base.html.twig' %}

{% block title %}{{ 'New Help'|trans }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('editor') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('editor') }}
{% endblock %}

{% block body %}
    <h1>{{ 'Create new Help'|trans }}</h1>

    {{ include('help/_form.html.twig') }}

    <div class="btn-group">
        <a class="btn btn-outline-secondary" href="{{ path('help_index') }}">{{ 'Back to list'|trans }}</a>
    </div>
{% endblock %}
