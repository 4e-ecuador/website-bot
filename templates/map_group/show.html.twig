{% extends 'base.html.twig' %}

{% block title %}MapGroup{% endblock %}

{% block body %}
    <h1>MapGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ map_group.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ map_group.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('map_group_index') }}">back to list</a>

    <a href="{{ path('map_group_edit', {'id': map_group.id}) }}">edit</a>

    {{ include('map_group/_delete_form.html.twig') }}
{% endblock %}
