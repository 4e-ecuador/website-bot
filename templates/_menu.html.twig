<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('default') }}">4E</a>


    {% if is_granted('ROLE_AGENT') %}
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <div class="navbar-nav">
                <ul class="nav navbar-nav">
                    <li class="nav-item"><a class="nav-link" href="{{ path('agent-map') }}">{{ 'Map'|trans }}</a></li>
                    <li class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown"
                           role="button">{{ 'Stats'|trans }}</a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href={{ path('stats_leaderboard') }}>{{ 'Leaderboard'|trans }}</a>
                            <a class="dropdown-item" href={{ path('stats_by_date') }}>{{ 'Medals by date'|trans }}</a>
                        </div>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href={{ path('agent_index') }}>{{ 'Agents'|trans }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href={{ path('default_events') }}>{{ 'Events'|trans }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href={{ path('help_index') }}>{{ 'Help'|trans }}</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item dropdown">
                            <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button">
                                {{ 'Admin'|trans }}
                            </a>
                            <div class="dropdown-menu">
                                <a class="dropdown-item" href="{{ path('user_index') }}">
                                    {{ 'Users'|trans }}
                                </a>
                                <a class="dropdown-item" href={{ path('agent_stat_index') }}>
                                    {{ 'Stats'|trans }}
                                </a>
                                <a class="dropdown-item" href="{{ path('comment_index') }}">
                                    {{ 'Coments'|trans }}
                                </a>
                                <a class="dropdown-item" href="{{ path('event_index') }}">
                                    {{ 'Events'|trans }}
                                </a>
                                <a class="dropdown-item" href="{{ path('challenge_index') }}">
                                    {{ 'Challenges'|trans }}
                                </a>
                                <a class="dropdown-item" href="{{ path('ingress_event_index') }}">
                                    {{ 'Ingress Events'|trans }}
                                </a>
                                <a class="dropdown-item" href="{{ path('map_group_index') }}">
                                    {{ 'Map groups'|trans }}
                                </a>
                                <a class="dropdown-item" href="{{ path('import') }}">
                                    {{ 'Import Agents'|trans }}
                                </a>
                                <a class="dropdown-item" href="{{ path('test_stat_index') }}">
                                    TEST stats
                                </a>
                                <a class="dropdown-item" href="{{ path('test') }}">
                                    TEST
                                </a>
                            </div>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>

    {% endif %}

    <div class="ml-auto">
        <ul class="navbar-nav ml-auto">
            {% if is_granted('ROLE_USER') %}
                <li class="nav-item dropdown">
                    <a style="color: #fff;" href="#" class="nav-link dropdown-toggle" data-toggle="dropdown"
                       role="button">
                        {{ app.user.username }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right bg-dark">
                        <a class="dropdown-item nav-link" href={{ path('app_account') }}>
                            <span class="oi oi-person"></span> {{ 'My Profile'|trans }}
                        </a>
                        <a class="dropdown-item nav-link" href={{ path('my_stats') }}>
                            <span class="oi oi-graph"></span> {{ 'My Stats'|trans }}
                        </a>
                        <a class="dropdown-item nav-link" href={{ path('stat_import') }}>
                            <span class="oi oi-cloud-upload"></span> {{ 'Import Stats'|trans }}
                        </a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item nav-link" href="{{ path('app_logout') }}">
                            <span class="oi oi-account-logout"></span> {{ 'Logout'|trans }}
                        </a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item">
                    {% if 'dev' == app.environment %}
                        <a href="{{ path('app_login') }}" class="nav-link">dev login</a>
                    {% else %}
                        <a href="{{ path('connect_google_start') }}" class="nav-link">{{ 'Login'|trans }}</a>
                    {% endif %}
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
