#!/bin/bash
#
# Startup script
#

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && cd .. && pwd )"

cd "$DIR" || echo 'cannot cd :('

echo "Starting dev env in $DIR ..."

docker-compose up -d

symfony server:start -d

symfony open:local
