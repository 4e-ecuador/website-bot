<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('default') }}">4E</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <div class="navbar-nav">
            <ul class="nav navbar-nav">

                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item"><a class="nav-link" href={{ path('agent_index') }}>Agents</a></li>

{#                {% if app.user and app.user.role == 'ROLE_ADMIN' %}#}
                    <li class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button">Admin</a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path('import') }}">Import</a>
                            <div class="dropdown-divider"></div>
                            <h6 class="dropdown-header">Mail</h6>
                            <a class="dropdown-item" href="#">Transactions</a>
                        </div>
                    </li>
                {% endif %}

            </ul>
        </div>

        <div class="ml-auto">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a class="nav-link" accesskey="l" href="{{ path('app_logout') }}" title="Logout">
                    <span class="oi oi-account-logout"></span> Logout
                </a>
            {% else %}
                <a class="nav-link" accesskey="l" href="{{ path('app_login') }}" title="Login">
                    <span class="oi oi-account-login"></span> Login
                </a>
            {% endif %}
        </div>

    </div>
</nav>
