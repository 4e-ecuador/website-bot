{{ form_start(form) }}

<div class="row">
    <div class="col-sm">
        {{ form_row(form.email) }}
    </div>
    <div class="col-sm">
    </div>
</div>
<div class="row">
    <div class="col-sm">
        {{ form_row(form.agent) }}
    </div>
    <div class="col-sm">
        {{ form_row(form.roles) }}
    </div>
</div>

{{ form_rest(form) }}

<hr>

<button class="btn btn-outline-success" title="{{ button_label|default('Save'|trans) }}">
    <span class="oi oi-check"></span>
</button>

{{ form_end(form) }}
