{% extends 'base.html.twig' %}

{% block title %}Edit IngressEvent{% endblock %}

{% block body %}
    <div class="float-right btn-group">
        <a class="btn btn-outline-secondary" href="{{ path('ingress_event_index') }}"
           title="{{ 'Back to list'|trans }}">
        <span class="oi oi-arrow-left">
        </a>

        {{ include('ingress_event/_delete_form.html.twig') }}

    </div>

    <h1>Edit IngressEvent</h1>

    {{ include('ingress_event/_form.html.twig', {'button_label': 'Update'}) }}

{% endblock %}
