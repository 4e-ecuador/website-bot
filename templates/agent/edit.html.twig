{% extends 'base.html.twig' %}

{% block title %}{{ 'Edit Agent'|trans }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('map-edit') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('map-edit') }}
{% endblock %}

{% block body %}
    <div class="float-right">

        <a class="btn btn-outline-secondary" href="{{ path('agent_index') }}">{{ 'Back to list'|trans }}</a>
        {#        {{ include('agent/_delete_form.html.twig') }}#}
    </div>
    <h1>{{ 'Edit Agent'|trans }}</h1>

    {{ include('agent/_form.html.twig', {'button_label': 'Actualizar'}) }}

{% endblock %}
